import { GetRegionNamePipe } from './get-region-name.pipe';

describe('GetRegionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetRegionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
